export enum Role {
  TRADESPERSON = 'tradesperson',
  EMPLOYER = 'employer',
}

export enum Status {
  ONLINE = 'online',
  IDLE = 'idle',
}

export enum JobApplicationStatus {
  PENDING = 'pending',
  REVIEWED = 'reviewed',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
